"""Calculated/derived sensor descriptions (placeholder for future derived sensors)."""
from __future__ import annotations

# This module is reserved for sensors that are computed from combinations of
# raw register values rather than read directly from a single register.
# Add SigenSensorDescription instances to CALCULATED_SENSORS as the integration
# grows to include inverter and battery data.

CALCULATED_SENSORS: tuple = ()
